# Outcome finding

Write the finding for the one protocol outcome in the message, using only the extraction and
appraisal rows the packet shows and the unreported-outcome decisions that say what is missing.

Cite only extraction ids from the packet. Claim a comparative effect only from between-group
results; classify anything else as context. Call `submit_finding` once with a single `result`
object.
